//this interface is for calculating statistics
public interface Calculable {
    double calculateAverage();              // calculates average
    double calculateGPA();                  // calculates GPA (4.0 scale)
}
